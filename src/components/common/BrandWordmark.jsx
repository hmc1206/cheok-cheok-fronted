// "척척" 워드마크 텍스트. 원래는 홈/로그인/인증 콜백 화면이 각자 "아이콘
// 이미지 + 척척 텍스트" 헤더를 따로 구현해서 같은 브랜드 텍스트가 3곳에
// 중복돼 있었다(요청사항: 아이콘 이미지는 삭제하고 텍스트만 남기며, 이
// 기회에 공통 컴포넌트로 통합해 앞으로 여러 화면이 항상 같은 텍스트를 쓰도록
// 함). 화면마다 크기/자간/색상은 계속 다르게 유지하기로 했으므로(요청사항:
// 기존 값 그대로 유지 — 예: 홈은 18px, 로그인/콜백은 19px) 스타일은 각
// 화면이 className으로 그대로 지정한다 — 이 컴포넌트가 강제하는 건 "척척"
// 이라는 문자열 자체와 "아이콘 없이 텍스트만 나온다"는 구조뿐이다.
export function BrandWordmark({ className }) {
  return <span className={className}>척척</span>
}
