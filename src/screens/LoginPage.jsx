// TODO(feature/fe-login): 구글 로그인 페이지(GoogleLogin 팝업 + idToken 검증) 구현 예정.
// core 브랜치에서는 라우팅이 끊기지 않도록 최소 placeholder만 둔다.
export function LoginPage() {
  return (
    <main className="p-6">
      <h1 style={{ fontSize: 'var(--font-size-xl)' }}>로그인</h1>
      <p>준비 중입니다.</p>
    </main>
  )
}
